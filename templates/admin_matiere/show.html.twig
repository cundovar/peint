{% extends 'base.html.twig' %}

{% block title %}Matiere{% endblock %}

{% block body %}
    <h1>Matiere</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ matiere.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ matiere.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_matiere_index') }}">retour liste</a>

    <a href="{{ path('app_admin_matiere_edit', {'id': matiere.id}) }}">editer</a>

    {{ include('admin_matiere/_delete_form.html.twig') }}
{% endblock %}
